= form_with(model: @post, local: true, url: choose_new_or_edit) do |form|
  - if @post.errors.any?
    #error_explanation
      h2
        = @post.errors.count
        | 件のエラーがあります。
      ul
        - @post.errors.full_messages.each do |msg|
          li = msg

  .post_content
    = form.label :content
    = form.text_field :content
  = form.submit "投稿する"
  br
  = link_to "一覧へ行く", posts_path
